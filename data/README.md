# Data Directory

Use this folder for demo materials and local generated data.

- `samples/`: small notices, advisor notes, resume snippets, and interview examples.
- `uploads/`: local user uploads, ignored by Git.
- `vector_store/`: local vector index files, ignored by Git.

Keep sample files small and anonymized so they can be committed safely.
